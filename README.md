# sql
This project demonstrates the use of SQL subqueries in SELECT, WHERE, and FROM clauses. It shows how nested queries can be used to retrieve filtered, summarized, and meaningful data from relational databases using MySQL Workbench or DB Browser for SQLite.
